<Window x:Class="Finance.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Finance"
        mc:Ignorable="d"
        Title="Finance" Height="450" Width="800" WindowStartupLocation="CenterScreen" MinWidth="600" MinHeight="400">

    <DockPanel>
        <Menu IsMainMenu="True" DockPanel.Dock="Top">
            <MenuItem Header="_Zadání dat"/>
            <MenuItem Header="_Přehled dat"/>
            <MenuItem Header="Ú_vodní obrazovka"/>
            <MenuItem Header="_Nastavení úvodní obrazovky"/>
            <MenuItem Header="_O programu"/>
            <MenuItem Header="_Ukončit" Command="Close"/>
        </Menu>
    </DockPanel>

    <Window.InputBindings>
        <KeyBinding Key="F4" Modifiers="Alt" Command="Close"/>
    </Window.InputBindings>

    <Window.CommandBindings>
        <CommandBinding Command="Close" Executed="Close_CommandBinding_Executed" CanExecute="Close_CommandBinding_CanExecute"/>
    </Window.CommandBindings>

</Window>
